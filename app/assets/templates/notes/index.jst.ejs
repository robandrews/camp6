<% notes.each(function(note){ %>  
  <div class="note-index">
    <h5> <a href="#notes/<%=note.id%>">Show</a> </h5>
    <p> <%=note.get("body")%> </p>
    <em> Authored by <%= note.get("author_id") %> on <%=note.get("updated_at")%> </em>
  </div>
<% }) %>